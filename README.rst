Django Contact Recipients
=========================

A small app for managing the recipients of contact forms via the Django
admin site.

Tests
-----

To run the tests you'll need to install `django-dynamic-fixture`::

    pip install django-dynamic-fixture
